import credentials from "./credentials";

const config = {
  credentials,
};

// Exportar también las credenciales individualmente para facilitar el uso
export { credentials };
export default config;
